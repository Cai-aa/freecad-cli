# freecad agent-harness

CLI-Anything style harness scaffold for FreeCAD.

This layer documents how `freecad-cli` can evolve toward a fuller `cli_anything.freecad` package while keeping the current `fc` CLI as the practical execution frontend.
